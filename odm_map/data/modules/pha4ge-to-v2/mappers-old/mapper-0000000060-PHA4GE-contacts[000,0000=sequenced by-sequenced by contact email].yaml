class_derivations:
  contacts[000,0000=sequenced by-sequenced by contact email]:
    name: contacts[000,0000=sequenced by-sequenced by contact email]
    populated_from: PHA4GE
    slot_derivations:
      organizationID:
        name: organizationID
        populated_from: sequenced_by
      email:
        name: email
        populated_from: sequenced_by_contact_email
      firstName:
        name: firstName
        expr: "def get_name(full_name, get_first):\n    names = full_name.strip().rsplit(\"\
          \ \", 1)\n    idx = 0 if get_first else 1\n    return names[idx] if len(names)\
          \ > idx else \"\"\ntarget = get_name(src.sequenced_by_contact_name, True)"
      lastName:
        name: lastName
        expr: "def get_name(full_name, get_first):\n    names = full_name.strip().rsplit(\"\
          \ \", 1)\n    idx = 0 if get_first else 1\n    return names[idx] if len(names)\
          \ > idx else \"\"\ntarget = get_name(src.sequenced_by_contact_name, False)"
  Container:
    name: Container
    slot_derivations:
      contacts[000,0000=sequenced by-sequenced by contact email]:
        populated_from: PHA4GE
enum_derivations: {}
