class_derivations:
  contacts[000,0000=sample_collected_by-sample_collector_contact_email]:
    name: contacts[000,0000=sample_collected_by-sample_collector_contact_email]
    populated_from: PHA4GE
    slot_derivations:
      organizationID:
        name: organizationID
        populated_from: sample_collected_by
      _extra_organizationID_tag:
        name: _extra_organizationID_tag
        expr: '''sample collected by'''
      _extra_contactID_tag:
        name: _extra_contactID_tag
        expr: '''sample collected by'''
      email:
        name: email
        populated_from: sample_collector_contact_email
      firstName:
        name: firstName
        expr: "def get_name(full_name, get_first):\n    if not full_name:\n      \
          \  return \"\"\n    names = full_name.strip().rsplit(\" \", 1)\n    idx\
          \ = 0 if get_first else 1\n    return names[idx] if len(names) > idx else\
          \ \"\"\ntarget = get_name(src.sample_collection_data_steward_name, True)"
      lastName:
        name: lastName
        expr: "def get_name(full_name, get_first):\n    if not full_name:\n      \
          \  return \"\"\n    names = full_name.strip().rsplit(\" \", 1)\n    idx\
          \ = 0 if get_first else 1\n    return names[idx] if len(names) > idx else\
          \ \"\"\ntarget = get_name(src.sample_collection_data_steward_name, False)"
  Container:
    name: Container
    slot_derivations:
      contacts[000,0000=sample_collected_by-sample_collector_contact_email]:
        populated_from: PHA4GE
enum_derivations:
  ODMv3_enum_from_NullValueMenu:
    name: ODMv3_enum_from_NullValueMenu
    mirror_source: false
    populated_from: NullValueMenu
    permissible_value_derivations:
      NA:
        name: NA
        populated_from: Not Applicable [GENEPIO:0001619]
      miss:
        name: miss
        populated_from: Missing [GENEPIO:0001618]
      dnc:
        name: dnc
        populated_from: Not Collected [GENEPIO:0001620]
      undisc:
        name: undisc
        populated_from: Not Provided [GENEPIO:0001668]
      restrict:
        name: restrict
        populated_from: Restricted Access [GENEPIO:0001810]
